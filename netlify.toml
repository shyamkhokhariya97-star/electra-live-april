[build]
  command = "npm run build"
  publish = "build"

# Pin Node for CRA (react-scripts 5) stability on Netlify.
[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

